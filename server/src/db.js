import { connect } from '@tursodatabase/serverless';
import bcrypt from 'bcryptjs';

const url=process.env.TURSO_DATABASE_URL,authToken=process.env.TURSO_AUTH_TOKEN;
if(!url||!authToken)throw new Error('Missing TURSO_DATABASE_URL or TURSO_AUTH_TOKEN. Add both to server/.env locally and Render Environment in production.');
const config={url,authToken};
export const db=connect(config);
const pool=Array.from({length:4},()=>connect(config));let poolCursor=0;
async function execute(method,sql,params=[]){
  let lastError;
  for(let attempt=0;attempt<3;attempt++){
    const index=poolCursor++%pool.length;
    try{return await pool[index][method](sql,...params);}
    catch(error){
      lastError=error;
      const message=String(error?.message||error),cause=String(error?.cause?.code||'');
      const transient=message.includes('fetch failed')||message.includes('ECONNRESET')||message.includes('HTTP error! status: 404')||cause==='ECONNRESET'||cause==='ETIMEDOUT';
      if(!transient)throw error;
      pool[index]=connect(config);
      if(attempt<2)await new Promise(resolve=>setTimeout(resolve,150*(attempt+1)));
    }
  }
  throw lastError;
}
export const queryOne=(sql,params=[])=>execute('get',sql,params);
export const queryAll=(sql,params=[])=>execute('all',sql,params);
export const run=(sql,params=[])=>execute('run',sql,params);
export async function withTransaction(work){const transaction=db.transactionAsync(async tx=>work({queryOne:(sql,params=[])=>tx.get(sql,...params),queryAll:(sql,params=[])=>tx.all(sql,...params),run:(sql,params=[])=>tx.run(sql,...params)}));return transaction.immediate();}

const schemaStatements=[
`CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,email TEXT NOT NULL UNIQUE COLLATE NOCASE,password_hash TEXT NOT NULL,role TEXT NOT NULL CHECK(role IN ('ADMIN','SALES')),active INTEGER NOT NULL DEFAULT 1,phone TEXT,designation TEXT,city TEXT,bio TEXT,permissions_configured INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT (datetime('now')))`,
`CREATE TABLE IF NOT EXISTS leads(id INTEGER PRIMARY KEY AUTOINCREMENT,company_name TEXT,contact_name TEXT NOT NULL,phone TEXT,email TEXT,city TEXT,source TEXT,requirement TEXT,box_size TEXT,quantity INTEGER DEFAULT 0,quantity_range TEXT,per_box_budget REAL DEFAULT 0,finalized INTEGER NOT NULL DEFAULT 0,sample_sent INTEGER NOT NULL DEFAULT 0,order_sent INTEGER NOT NULL DEFAULT 0,quote_sent_at TEXT,finalized_at TEXT,sample_sent_at TEXT,order_sent_at TEXT,estimated_value REAL DEFAULT 0,status TEXT NOT NULL DEFAULT 'NEW_LEAD',assigned_to INTEGER,next_followup_at TEXT,last_followup_at TEXT,notes TEXT,created_by INTEGER,created_at TEXT NOT NULL DEFAULT (datetime('now')),updated_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(assigned_to) REFERENCES users(id) ON DELETE SET NULL,FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS followups(id INTEGER PRIMARY KEY AUTOINCREMENT,lead_id INTEGER NOT NULL,user_id INTEGER NOT NULL,type TEXT NOT NULL DEFAULT 'CALL',followup_status TEXT,outcome TEXT,note TEXT,followup_at TEXT NOT NULL,next_followup_at TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE CASCADE,FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS purchase_orders(id INTEGER PRIMARY KEY AUTOINCREMENT,po_number TEXT NOT NULL UNIQUE,lead_id INTEGER NOT NULL,customer_json TEXT NOT NULL,items_json TEXT NOT NULL,place_of_supply TEXT,subtotal REAL NOT NULL DEFAULT 0,gst_rate REAL NOT NULL DEFAULT 5,tax_type TEXT NOT NULL DEFAULT 'IGST',tax_amount REAL NOT NULL DEFAULT 0,total REAL NOT NULL DEFAULT 0,advance_amount REAL NOT NULL DEFAULT 0,pending_amount REAL NOT NULL DEFAULT 0,status TEXT NOT NULL DEFAULT 'CREATED',created_by INTEGER,created_at TEXT NOT NULL DEFAULT (datetime('now')),manual_invoice INTEGER NOT NULL DEFAULT 0,FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE RESTRICT,FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS tax_invoices(id INTEGER PRIMARY KEY AUTOINCREMENT,invoice_number INTEGER NOT NULL UNIQUE,purchase_order_id INTEGER NOT NULL UNIQUE,invoice_json TEXT NOT NULL,created_by INTEGER,created_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(purchase_order_id) REFERENCES purchase_orders(id) ON DELETE RESTRICT,FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS document_sequences(name TEXT PRIMARY KEY,next_value INTEGER NOT NULL)`,
`CREATE TABLE IF NOT EXISTS document_settings(id INTEGER PRIMARY KEY CHECK(id=1),settings_json TEXT NOT NULL,updated_at TEXT NOT NULL DEFAULT (datetime('now')))`,
`CREATE TABLE IF NOT EXISTS masters(id INTEGER PRIMARY KEY AUTOINCREMENT,category TEXT NOT NULL,code TEXT NOT NULL,label TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1,sort_order INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT (datetime('now')),UNIQUE(category,code))`,
`CREATE TABLE IF NOT EXISTS lead_progress(lead_id INTEGER NOT NULL,code TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT (datetime('now')),PRIMARY KEY(lead_id,code),FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS sheet_integrations(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL DEFAULT 'Google Ads Leads',sheet_url TEXT NOT NULL,sheet_id TEXT NOT NULL,gid TEXT NOT NULL DEFAULT '0',enabled INTEGER NOT NULL DEFAULT 0,interval_minutes INTEGER NOT NULL DEFAULT 5,default_source TEXT NOT NULL DEFAULT 'Google Ads',assigned_to INTEGER,assignment_mode TEXT NOT NULL DEFAULT 'FIXED',assignment_user_ids_json TEXT NOT NULL DEFAULT '[]',leads_per_user INTEGER NOT NULL DEFAULT 1,assignment_cursor INTEGER NOT NULL DEFAULT 0,mapping_json TEXT NOT NULL DEFAULT '{}',last_sync_at TEXT,last_success_at TEXT,last_error TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),updated_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(assigned_to) REFERENCES users(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS sheet_imported_rows(id INTEGER PRIMARY KEY AUTOINCREMENT,integration_id INTEGER NOT NULL,row_key TEXT NOT NULL,row_number INTEGER,lead_id INTEGER,imported_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(integration_id) REFERENCES sheet_integrations(id) ON DELETE CASCADE,FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE SET NULL,UNIQUE(integration_id,row_key))`,
`CREATE TABLE IF NOT EXISTS sheet_sync_logs(id INTEGER PRIMARY KEY AUTOINCREMENT,integration_id INTEGER NOT NULL,trigger_type TEXT NOT NULL DEFAULT 'MANUAL',status TEXT NOT NULL,rows_found INTEGER NOT NULL DEFAULT 0,imported_count INTEGER NOT NULL DEFAULT 0,updated_count INTEGER NOT NULL DEFAULT 0,duplicate_count INTEGER NOT NULL DEFAULT 0,skipped_count INTEGER NOT NULL DEFAULT 0,error_message TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(integration_id) REFERENCES sheet_integrations(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS lead_notifications(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,lead_id INTEGER NOT NULL,event_key TEXT NOT NULL UNIQUE,read_at TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE,FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS activity_events(id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL,lead_id INTEGER,event_type TEXT NOT NULL,title TEXT NOT NULL,details TEXT,amount REAL NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT (datetime('now')),FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE,FOREIGN KEY(lead_id) REFERENCES leads(id) ON DELETE SET NULL)`,
`CREATE TABLE IF NOT EXISTS user_permissions(user_id INTEGER NOT NULL,permission TEXT NOT NULL,created_at TEXT NOT NULL DEFAULT (datetime('now')),PRIMARY KEY(user_id,permission),FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE)`,
`CREATE INDEX IF NOT EXISTS idx_leads_assigned_to ON leads(assigned_to)`,`CREATE INDEX IF NOT EXISTS idx_leads_next_followup_at ON leads(next_followup_at)`,`CREATE INDEX IF NOT EXISTS idx_leads_status ON leads(status)`,`CREATE INDEX IF NOT EXISTS idx_followups_lead_id ON followups(lead_id)`,`CREATE INDEX IF NOT EXISTS idx_lead_progress_code ON lead_progress(code)`,`CREATE INDEX IF NOT EXISTS idx_sheet_imported_key ON sheet_imported_rows(integration_id,row_key)`,`CREATE INDEX IF NOT EXISTS idx_sheet_sync_logs_integration ON sheet_sync_logs(integration_id,created_at DESC)`,`CREATE INDEX IF NOT EXISTS idx_lead_notifications_user ON lead_notifications(user_id,read_at,created_at DESC)`,`CREATE INDEX IF NOT EXISTS idx_activity_events_user_date ON activity_events(user_id,created_at DESC)`];

const masterSeeds={LEAD_STATUS:[['NEW_LEAD','New Lead'],['CONTACTED','Contacted'],['REQUIREMENT_RECEIVED','Requirement Received'],['QUALIFIED','Qualified'],['NEED_SAMPLE','Need Sample'],['SAMPLE_SENT','Sample Sent'],['QUOTATION_SENT','Quotation Sent'],['NEGOTIATION','Negotiation'],['SAMPLE_DESIGN_APPROVAL','Sample / Design Approval'],['ORDER_CONFIRMED','Order Confirmed'],['PAYMENT_PENDING','Payment Pending'],['PAYMENT_RECEIVED','Payment Received'],['IN_PRODUCTION','In Production'],['READY_FOR_DISPATCH','Ready for Dispatch'],['DISPATCHED','Dispatched'],['DELIVERED','Delivered'],['NOT_INTERESTED','Not Interested'],['CLOSED_WON','Closed Won'],['CLOSED_LOST','Closed Lost']],LEAD_PROGRESS:[['NEED_SAMPLE','Need Sample'],['SAMPLE_SENT','Sample Sent'],['FINAL_QUOTE_SENT','Final Quote Sent'],['ORDER_FINAL','Order Final']],FOLLOWUP_TYPE:[['CALL','Call'],['WHATSAPP','WhatsApp'],['EMAIL','Email'],['MEETING','Meeting']],FOLLOWUP_STATUS:[['CALL_PENDING','Call Pending'],['WHATSAPP_PENDING','WhatsApp Pending'],['EMAIL_PENDING','Email Pending'],['NO_RESPONSE','No Response'],['CALL_BACK_LATER','Call Back Later'],['CALLBACK_SCHEDULED','Callback Scheduled'],['CATALOGUE_SHARED','Catalogue Shared'],['REQUIREMENT_AWAITED','Requirement Awaited'],['QUOTATION_PENDING','Quotation Pending'],['QUOTATION_FOLLOW_UP','Quotation Follow-up'],['INTERNAL_APPROVAL_PENDING','Internal Approval Pending'],['BUDGET_DISCUSSION','Budget Discussion'],['PRICE_NEGOTIATION','Price Negotiation'],['SAMPLE_FOLLOW_UP','Sample Follow-up'],['DESIGN_AWAITED','Design Awaited'],['DESIGN_APPROVAL_PENDING','Design Approval Pending'],['PO_AWAITED','PO Awaited'],['PAYMENT_FOLLOW_UP','Payment Follow-up'],['ON_HOLD','On Hold'],['NOT_INTERESTED','Not Interested'],['FOLLOW_UP_COMPLETED','Follow-up Completed']],LEAD_SOURCE:[['WEBSITE','Website'],['WHATSAPP','WhatsApp'],['REFERRAL','Referral'],['INSTAGRAM','Instagram'],['EXCEL_IMPORT','Excel Import'],['GOOGLE_ADS','Google Ads'],['MANUAL_PO','Manual PO']],CITY:[['DELHI','Delhi'],['NOIDA','Noida'],['GURUGRAM','Gurugram']],BOX_SIZE:[['6_PCS','6 pcs'],['12_PCS','12 pcs'],['24_PCS','24 pcs']],HSN_CODE:[['18069010','1806.90.10']],GST_RATE:[['5','5%'],['12','12%'],['18','18%']],TAX_TYPE:[['IGST','IGST'],['CGST_SGST','CGST + SGST']]};
const defaultPermissions=['PAGE_DASHBOARD','PAGE_LEADS','PAGE_FOLLOWUPS','PAGE_REPORTS','PAGE_ORDERS','SECTION_DASHBOARD_REVENUE','SECTION_DASHBOARD_FOLLOWUPS','ACTION_LEADS_CREATE','ACTION_LEADS_EDIT','ACTION_LEADS_EXPORT','ACTION_FOLLOWUPS_MANAGE','ACTION_ORDERS_CREATE','ACTION_ORDERS_DOWNLOAD','ACTION_ORDERS_INVOICE'];
async function ensureColumns(table,defs){const cols=new Set((await queryAll(`PRAGMA table_info(${table})`)).map(x=>x.name));for(const [name,def] of defs)if(!cols.has(name))await db.exec(`ALTER TABLE ${table} ADD COLUMN ${name} ${def}`);}
async function seedUser(name,email,password,role){if(!await queryOne('SELECT id FROM users WHERE email=?',[email]))await run('INSERT INTO users(name,email,password_hash,role) VALUES(?,?,?,?)',[name,email,bcrypt.hashSync(password,10),role]);}
let initialized=false;
export async function initDb(){if(initialized)return;await db.exec('PRAGMA foreign_keys=ON');await db.batch(schemaStatements);
 await ensureColumns('users',[['phone','TEXT'],['designation','TEXT'],['city','TEXT'],['bio','TEXT'],['permissions_configured','INTEGER NOT NULL DEFAULT 0']]);await ensureColumns('leads',[['box_size','TEXT'],['quantity','INTEGER DEFAULT 0'],['quantity_range','TEXT'],['per_box_budget','REAL DEFAULT 0'],['finalized','INTEGER NOT NULL DEFAULT 0'],['sample_sent','INTEGER NOT NULL DEFAULT 0'],['order_sent','INTEGER NOT NULL DEFAULT 0'],['quote_sent_at','TEXT'],['finalized_at','TEXT'],['sample_sent_at','TEXT'],['order_sent_at','TEXT']]);await ensureColumns('followups',[['followup_status','TEXT']]);await ensureColumns('sheet_integrations',[['assignment_mode',"TEXT NOT NULL DEFAULT 'FIXED'"],['assignment_user_ids_json',"TEXT NOT NULL DEFAULT '[]'"],['leads_per_user','INTEGER NOT NULL DEFAULT 1'],['assignment_cursor','INTEGER NOT NULL DEFAULT 0']]);await ensureColumns('sheet_sync_logs',[['updated_count','INTEGER NOT NULL DEFAULT 0']]);await ensureColumns('purchase_orders',[['manual_invoice','INTEGER NOT NULL DEFAULT 0']]);
 await db.batch([{sql:'INSERT OR IGNORE INTO document_sequences(name,next_value) VALUES(?,?)',args:['invoice',10001]},{sql:'INSERT OR IGNORE INTO document_sequences(name,next_value) VALUES(?,?)',args:['purchase_order',1]}]);await seedUser('CMA Admin','admin@chocomanualart.com','Admin@123','ADMIN');await seedUser('Demo Sales','sales@chocomanualart.com','Sales@123','SALES');
 const seeds=[];
 for(const [category,items] of Object.entries(masterSeeds))items.forEach(([code,label],sort)=>seeds.push({sql:'INSERT OR IGNORE INTO masters(category,code,label,sort_order) VALUES(?,?,?,?)',args:[category,code,label,sort]}));await db.batch(seeds);
 await run(`INSERT INTO sheet_integrations(name,sheet_url,sheet_id,gid,enabled,interval_minutes,default_source,mapping_json) SELECT 'Google Ads Leads','https://docs.google.com/spreadsheets/d/1JMhUXLAp0zxmm45e_uQiYszggIsw0EEAg_leCsqRA3Q/edit?usp=sharing','1JMhUXLAp0zxmm45e_uQiYszggIsw0EEAg_leCsqRA3Q','0',1,5,'Google Ads','{}' WHERE NOT EXISTS(SELECT 1 FROM sheet_integrations)`);
 const sales=await queryAll("SELECT id FROM users WHERE role='SALES' AND permissions_configured=0");for(const user of sales){await db.batch(defaultPermissions.map(permission=>({sql:'INSERT OR IGNORE INTO user_permissions(user_id,permission) VALUES(?,?)',args:[user.id,permission]})));await run('UPDATE users SET permissions_configured=1 WHERE id=?',[user.id]);}
 await db.batch(["UPDATE leads SET status='NEW_LEAD' WHERE status='NEW'","UPDATE leads SET status='QUALIFIED' WHERE status IN ('INTERESTED','FOLLOW_UP')","UPDATE leads SET status='QUOTATION_SENT' WHERE status='QUOTE_SENT'","UPDATE leads SET status='CLOSED_WON' WHERE status='WON'","UPDATE leads SET status='CLOSED_LOST' WHERE status='LOST'","UPDATE leads SET status='CONTACTED' WHERE status='NOT_REACHABLE'","UPDATE leads SET quote_sent_at=COALESCE(quote_sent_at,updated_at) WHERE status='QUOTATION_SENT'","UPDATE leads SET finalized_at=COALESCE(finalized_at,updated_at) WHERE finalized=1","UPDATE leads SET sample_sent_at=COALESCE(sample_sent_at,updated_at) WHERE sample_sent=1","UPDATE leads SET order_sent_at=COALESCE(order_sent_at,updated_at) WHERE order_sent=1"]);const maxPo=await queryOne("SELECT COALESCE(MAX(CAST(SUBSTR(po_number,4) AS INTEGER)),0)+1 next FROM purchase_orders WHERE po_number LIKE 'PO-%'");await run("UPDATE document_sequences SET next_value=MAX(next_value,?) WHERE name='purchase_order'",[maxPo.next]);initialized=true;console.log('Turso database initialized');}
export default db;
